# adapt-authoring-users

Manages system users — the accounts that sign in to the authoring tool. Stores user records and exposes them via a REST API; [roles](../adapt-authoring-roles) assigned to a user determine what they can do.

Extends `AbstractApiModule` from [adapt-authoring-core](../adapt-authoring-core).
